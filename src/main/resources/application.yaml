spring:
  application:
    name: inventario 

  datasource:
    url: jdbc:mysql://hopper.proxy.rlwy.net:41276/railway
    username: root
    password: PeXeSzgKbtOwSCZgIPFndeunMLyHoLZc
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true


logging:
  level:
    '[org.springframework.web]':  DEBUG
    '[org.hibernate.SQL]':  DEBUG
    '[org.hibernate.type.descriptor.sql.BasicBinder]': DEBUG

server:
  port: ${PORT:8080}
   